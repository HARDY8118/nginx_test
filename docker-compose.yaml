version: '3'

services:
  nginx:
    container_name: "server"
    image: nginx
    volumes:
      - ./static/:/usr/share/nginx/html/
      - ./nginx/:/etc/nginx/
    ports:
      - 80:80
      - 443:443
    # environment:
      # - NGINX_PORT=80  
      # - NGINX_PORT=443
